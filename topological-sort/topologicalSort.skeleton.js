/**
 * Given a directed, acyclic graph (represented as an adjacency list)
 * return an array containing a valid topological ordering of its vertexes.
 *
 * @param graph - An adjacency list for a directed, acyclic graph
 * @returns {any[]} - A valid topological ordering of the graph's vertexes
 */
function topologicalSort(graph) {
  // Your code code here
}
